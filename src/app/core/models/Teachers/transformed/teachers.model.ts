export interface Teachers {
    email: string,
    password: string,
    name: string,
    phone: number,
    idCard: string,
    grade: string,
}
/* Este código define la interfaz Teachers, la cual tiene las siguientes propiedades:

email: de tipo string.
password: de tipo string.
name: de tipo string.
phone: de tipo number.
idCard: de tipo string.
grade: de tipo string.
Esto indica que la aplicación tiene una entidad llamada "maestros" y que cada instancia de maestro tendrá esas propiedades.

Es un Interface transformado de ApiTeachers quitando las propiedades menos relevantes

 */