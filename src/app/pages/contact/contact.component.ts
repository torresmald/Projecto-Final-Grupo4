import { Component } from '@angular/core';

@Component({
  selector: 'app-contact',
  templateUrl: './contact.component.html',
  styleUrls: ['./contact.component.scss']
})

export class ContactComponent{
  
  }
/* Define un componente llamado ContactComponent y utiliza el decorador @Component para definir la información de la clase. Esto incluye el selector que se usará para agregar el componente a la vista, la ubicación del archivo HTML que contiene los elementos de la vista y la ubicación del archivo SCSS que contiene los estilos del componente. */