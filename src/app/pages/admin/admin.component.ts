import { Component } from '@angular/core';

@Component({
  selector: 'app-admin',
  templateUrl: './admin.component.html',
  styleUrls: ['./admin.component.scss']
})
export class AdminComponent {

}
/* define una clase llamada AdminComponent y utiliza el decorador @Component para definir la información de la clase. Esto incluye el selector que se usará para agregar el componente a la vista, la ubicación del archivo HTML que contiene los elementos de la vista y la ubicación del archivo SCSS que contiene los estilos del componente. */